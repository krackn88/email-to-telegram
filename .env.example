# Copy to .env and fill in. Never commit .env, credentials.json, or token.json.

# Gmail: your address. Login is via OAuth (no password in .env).
# Run once: python main.py auth  (opens browser to sign in with Google)
IMAP_USER=your_email@gmail.com

# Optional: use App Password instead of OAuth (set this to skip OAuth)
# IMAP_PASSWORD=your_16_char_app_password

# Telegram: create a bot with @BotFather; get recipient's chat_id via getUpdates
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=recipient_chat_id

# Optional: only forward emails whose subject contains this (default: Claude)
# SUBJECT_FILTER=Claude
